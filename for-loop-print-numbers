using System;

class Program
{
    static void Main()
    {
        // For loop starts at 1, runs while i is less than or equal to 10
        for (int i = 1; i <= 10; i++)
        {
            Console.WriteLine(i);
        }
    }
}
